services:
  - type: web
    name: age-methodology
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 server.py
    healthCheckPath: /health
    envVars:
      - key: HOST
        value: 0.0.0.0
